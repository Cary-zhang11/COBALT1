diff --git a/.claude/skills/prd-to-tests-new/SKILL.md b/.claude/skills/prd-to-tests-new/SKILL.md
index 2e6d2e0..f6b69a9 100644
--- a/.claude/skills/prd-to-tests-new/SKILL.md
+++ b/.claude/skills/prd-to-tests-new/SKILL.md
@@ -3,11 +3,11 @@ name: prd-to-tests-new
 description: 将业务需求文档（PRD）转换为结构化测试用例文档。当用户提供 Word (.docx) 格式的需求文档并要求生成测试用例、测试场景、测试方案时使用本技能。也适用于需要分析 PRD 内容、提取功能点、识别测试维度、生成覆盖功能/边界/异常/安全/性能/兼容性的完整测试用例的场景。即使用户没有明确提到"PRD"或"测试用例"，只要涉及需求分析转测试的工作都应触发此技能。
 ---
 
-# PRD → 测试用例生成
+# PRD → 三方分析 + 开发冒烟用例 + 全量测试用例
 
 ## 角色定位
 
-你是资深 QA 测试专家，擅长从业务需求中提取测试要点，生成结构化、可执行、可回归的测试用例文档。
+你是资深 QA 测试专家，负责：(1) 以产品/架构/测试三视角深度分析 PRD；(2) 基于分析结论生成精简开发冒烟用例；(3) 生成覆盖全优先级的结构化测试用例。三方分析结论贯穿全流程，风险点和矛盾点同时纳入冒烟用例和全量用例，确保两套产出相互印证。
 
 ---
 
@@ -16,21 +16,31 @@ description: 将业务需求文档（PRD）转换为结构化测试用例文档
 **每次使用本技能时，必须严格按以下顺序执行：**
 
 1. 读取本文件 `SKILL.md` 全文
-2. 读取全部参考文件（以下 3 个文件，缺一不可）
-   - `references/test_dimensions.md` — 8 个测试维度的详细定义、覆盖范围、拆分规则、裁剪规则
+2. 读取全部参考文件（以下 8 个文件，缺一不可）
+   - `references/test_dimensions.md` — 测试维度定义、触发判定、裁剪规则
    - `references/generation_rules.md` — 用例编写规范（模块划分/分组/校验链/模板/防漏等全部规则）
-   - `references/output_template.md` — 输出文档结构模板、冒烟清单选取规则、完整性自检清单
+   - `references/output_template.md` — 输出文档结构模板、完整性自检清单
+   - `references/smoke_analysis.md` — 三方分析 + 红蓝对抗规则
+   - `references/smoke_decomposition.md` — P0 功能点拆分规则
+   - `references/smoke_generation.md` — 开发冒烟用例生成规则
+   - `references/smoke_output.md` — 冒烟用例输出格式与校验规则
+   - `references/generation_rules.md` — 全量用例生成规则（第二次引用，Step 6 用）
 3. 确认已理解全部规则后，方可开始执行后续步骤
 
 **规则章节对应表：**
 
-| 步骤 | 用到的规则章节 |
+| 步骤 | 用到的规则文件 |
 |------|---------------|
-| Step 2.1 模块划分 | `generation_rules.md` → 模块划分规则、PRD自身矛盾检测规则 |
-| Step 2.3 场景分组 | `generation_rules.md` → 模块内分组规则 |
-| Step 2.4 校验链分组 | `generation_rules.md` → 校验链分组规则 |
-| Step 4 用例生成 | `generation_rules.md` → 用例模板/合并规则/拆分规则/防漏规则/来源标注/优先级分配 |
-| Step 5 输出校验 | `output_template.md` → 文档结构模板/完整性自检清单 |
+| Step 2.1-2.3 三方分析 | `smoke_analysis.md` → 三方独立分析 + 红蓝对抗 + 结论汇总 |
+| Step 2.4 P0 拆分 | `smoke_decomposition.md` → P0 功能点拆分 + 问题映射表 |
+| Step 3.1 冒烟用例 | `smoke_generation.md` → 用例模板/步骤限制/前置最小化 |
+| Step 3.2 冒烟校验 | `smoke_output.md` → 输出格式/完整性校验/双文档一致性 |
+| Step 4 模块划分 | `generation_rules.md` → 模块划分规则、PRD自身矛盾检测规则 |
+| Step 4 场景分组 | `generation_rules.md` → 模块内分组规则 |
+| Step 4 校验链 | `generation_rules.md` → 校验链分组规则 |
+| Step 5 维度识别 | `test_dimensions.md` → 12 个测试维度触发判定 |
+| Step 6 用例生成 | `generation_rules.md` → 用例模板/合并/拆分/防漏/来源标注/优先级分配 |
+| Step 7 输出校验 | `output_template.md` → 文档结构模板/完整性自检清单 |
 
 > 即使你对本技能非常熟悉，每次执行也**必须从头读取全部文件**。这是强制要求，不是可选建议。
 
@@ -45,31 +55,74 @@ description: 将业务需求文档（PRD）转换为结构化测试用例文档
 [WF:done:<节点名称>]
 ```
 
-**节点名称与步骤对应表（5 个核心大步骤）：**
+**节点名称与步骤对应表（8 个步骤）：**
 
 | 步骤 | 节点名称 | 插入时机 |
 |------|---------|---------|
 | Step 0-1: 解析与预加载 | `文档解析` | 规则预加载 + docx 转换 + 骨架 + 图文交叉读取全部完成后 |
-| Step 2-3: 需求分析 | `需求分析` | 模块划分 + 场景分组 + 维度识别全部完成后 |
-| Step 4: 用例生成 | `用例生成` | 全部用例生成完成后 |
-| Step 5 前半: 质量校验 | `质量校验` | 完整性自检（格式/覆盖/编号）完成后 |
-| Step 5 后半: 导出 | `导出格式` | md2xmind.py 调用完成后 |
+| Step 2: 三方分析 | `冒烟-三方分析` | 三方独立分析 + 红蓝对抗 + P0 拆分全部完成后 |
+| Step 3: 冒烟用例 | `冒烟-用例生成` | 冒烟用例生成 + 输出校验完成后 |
+| Step 3: 冒烟导出 | `冒烟-导出` | Excel 转换完成后 |
+| Step 4-5: 需求分析 | `需求分析` | 模块划分 + 场景分组 + 维度识别全部完成后 |
+| Step 6: 用例生成 | `用例生成` | 全部用例生成完成后 |
+| Step 7 前半: 质量校验 | `质量校验` | 完整性自检（格式/覆盖/编号）完成后 |
+| Step 7 后半: 导出 | `导出格式` | md2xmind.py 调用完成后 |
 
-**强制要求**：以上 5 个标记必须全部输出，不得遗漏。标记必须独占一行，不得与其他内容混在同一行。
+**强制要求**：以上 8 个标记必须全部输出，不得遗漏。标记必须独占一行，不得与其他内容混在同一行。
 
 ---
 
 ## 工作流程
 
 ```
-读取 PRD → 解析需求（含图片） → 模块划分（PRD表格行） → 场景分组 → 识别测试维度 → 生成用例 → 输出文档 → 自检
+PRD 文档
+    │
+    ▼
+Step 0-1: 规则预加载 + 文档解析     [WF:done:文档解析]
+    │
+    ▼
+Step 2: 三方分析 + 红蓝对抗         [WF:done:冒烟-三方分析]
+    │     + P0 功能拆分
+    │     (分析结论贯穿后续所有步骤)
+    ▼
+Step 3: 冒烟用例生成 + Excel 导出   [WF:done:冒烟-用例生成]
+    │     → _三方分析.md            [WF:done:冒烟-导出]
+    │     → _开发冒烟用例.md
+    │     → _开发冒烟用例.xlsx
+    │
+    ▼
+Step 4: 模块划分 + 场景分组          (继承三方结论)
+    │     + 校验链分组 + 矛盾检测
+    │
+Step 5: 测试维度识别                 (继承三方结论)  [WF:done:需求分析]
+    │
+    ▼
+Step 6: 全量用例生成                 (继承三方结论)  [WF:done:用例生成]
+    │
+    ▼
+Step 7: 质量校验 + XMind 导出       [WF:done:质量校验]
+          → _测试用例.md            [WF:done:导出格式]
+          → _测试用例.xmind
 ```
 
+**最终交付物（5 份文件）：**
+
+```
+{TASK_OUTPUT_DIR}/
+├── {需求名称}_三方分析.md           ← Step 2-3 产出
+├── {需求名称}_开发冒烟用例.md       ← Step 3 产出
+├── {需求名称}_开发冒烟用例.xlsx     ← Step 3 产出
+├── {需求名称}_测试用例.md           ← Step 6-7 产出
+└── {需求名称}_测试用例.xmind        ← Step 7 产出
+```
+
+---
+
 ## 执行步骤
 
 ### Step 0: 规则预加载
 
-在开始任何工作前，**必须先读取 `references/` 目录下所有参考文件一次**，理解完整的测试维度、用例生成规则和输出格式要求。
+在开始任何工作前，**必须先读取 `references/` 目录下所有参考文件一次**，理解完整的测试维度、用例生成规则、三方分析规则和冒烟用例生成规则。
 
 ### Step 1: 输入解析
 
@@ -160,12 +213,14 @@ description: 将业务需求文档（PRD）转换为结构化测试用例文档
 
    **重要原则**：
    - **先骨架后血肉**：先全文扫描建立需求点清单，再逐个需求点深入
-   - **全量纳入**：骨架阶段只做识别和归类，不做"是否功能"的内容判断。参数说明、tag定义、posid映射等非功能需求同样纳入骨架，由后续Step 2的模块划分规则决定处理方式
+   - **全量纳入**：骨架阶段只做识别和归类，不做"是否功能"的内容判断。参数说明、tag定义、posid映射等非功能需求同样纳入骨架，由后续 Step 4 的模块划分规则决定处理方式
    - **需求聚焦**：同一需求点的文案+图片集中处理，避免上下文断裂
    - **段落关联**：每张图片与其所属需求点的文案关联分析
+
 4. **图片类型判定**：
    - **信息密度高**（流程图、算法图、规则表）：执行树形结构对齐 → 最小信息单元提取 → 文案比对 → 提取特征自证
    - **信息密度低**（UI效果图、截图）：一句话摘要 + 差异判定
+
 5. **图文差异处理**：
    - 图片补充了关键规则 → 写进用例，追加 `【图片补充：xxx】` 标注，同时记入「待澄清问题」
    - 图文不一致 → 写进用例（以文案为基准），追加 `【图片不一致：xxx】` 标注，同时记入「待澄清问题」和「完整性检查报告-PRD自身矛盾检测」
@@ -183,37 +238,139 @@ Step 1 全部完成后，输出进度标记：`[WF:done:文档解析]`（注意
 - 模糊、矛盾或缺失的信息点（标记为待澄清）
 - 图片内容（流程图、界面截图、状态图等）—— 解析图片内容并据此生成用例
 
-### Step 2: 模块划分与场景分组
+---
+
+### Step 2: 三方分析 + 红蓝对抗 + P0 功能拆分
+
+基于 Step 1 的 PRD 解析结果（`_source.md` + `_images/`），按 `references/smoke_analysis.md` 和 `references/smoke_decomposition.md` 执行。
+
+> **本步骤的分析结论将贯穿后续 Step 4-6。** 关键风险点、矛盾点、测试重点同时纳入冒烟用例和全量用例。
+
+**2.1 三方多维度分析**
+
+按 `smoke_analysis.md` 第一阶段执行：
+
+- **产品专家**：业务逻辑链完整性、用户场景覆盖、功能边界与优先级、需求缺陷（矛盾/歧义/遗漏）
+- **架构师**：技术架构评估、性能与扩展性、安全与稳定性、接口完整性
+- **测试专家**：核心测试要点、风险点清单、边界情况、安全隐患
+
+**2.2 红蓝对抗**
+
+按 `smoke_analysis.md` 第二阶段执行：
+
+- QA → 产品：挑战业务逻辑歧义、被滥用可能、用户误解风险
+- QA → 架构：质疑高并发/宕机/极端情况下的系统表现
+- 架构 → 产品：评估技术实现成本、指出过度设计或不可行环节
+- 每个对抗方向至少提出 2 个具体问题，总计 ≥3 个真实问题
+
+**2.3 结论汇总**
+
+按 `smoke_analysis.md` 第三阶段执行：
+
+- 关键风险表（序号/风险描述/涉及角色/严重程度/建议措施）
+- 架构建议
+- 产品建议
+- **测试重点**（后续 Step 5 维度识别中优先覆盖对应维度）
+- **冒烟测试范围建议**（建议纳入冒烟的核心功能点）
+
+**2.4 P0 功能拆分**
+
+按 `smoke_decomposition.md` 执行，仅拆分 P0 功能点：
+
+- 功能模块树（仅 P0，标注依赖关系）
+- 对抗性验证（每个 P0 功能点自问：异常路径/边界条件/并发竞态/失败回滚/权限越界）
+- 业务流程拆分（入口条件/正常路径/退出条件，仅 Happy Path）
+- 数据变更点识别（创建/读取/更新/删除）
+- **问题映射表**（三方分析问题 → P0 功能点 → 覆盖状态）：此表在后续 Step 4.2 需求提取清单中交叉校验，确保问题不遗漏
+
+输出进度标记：`[WF:done:冒烟-三方分析]`
+
+---
+
+### Step 3: 开发冒烟用例生成 + Excel 导出
+
+基于 Step 2 的三方分析结论和 P0 功能拆分，按 `references/smoke_generation.md` 和 `references/smoke_output.md` 执行。
+
+**3.1 冒烟用例生成**
+
+按 `smoke_generation.md` 执行：
+
+- **数量**：5-20 条，全部 P0（核心阻断级）
+- **步骤**：每条 ≤5 步，精简可执行
+- **前置**：开发本地可快速准备（允许：已登录/已创建测试数据；禁止：1000 条数据初始化/生产环境配置）
+- **溯源**：每条用例标注「三方关联」字段，链接到 Step 2 分析结论中的风险项编号
+- **覆盖**：Step 2 中的关键风险必须有对应冒烟用例
+
+**3.2 输出校验**
+
+按 `smoke_output.md` 执行：
+
+- 三方分析报告校验（三视角完整、红蓝对抗 ≥3 问题、结论含冒烟范围建议）
+- 冒烟用例文档校验（编号无重复 `SMK-{模块}-{序号}`、字段齐全、步骤 ≤5、覆盖率检查）
+- 双文档一致性检查（三方关联可追溯、术语统一）
+
+**3.3 写入文件**
+
+- 三方分析报告 → `{TASK_OUTPUT_DIR}/{需求名称}_三方分析.md`
+- 冒烟用例 → `{TASK_OUTPUT_DIR}/{需求名称}_开发冒烟用例.md`
+
+输出进度标记：`[WF:done:冒烟-用例生成]`
+
+**3.4 Excel 转换**
+
+```bash
+python "{SKILL_DIR}/scripts/md2excel.py" "{TASK_OUTPUT_DIR}/{需求名称}_开发冒烟用例.md" "{TASK_OUTPUT_DIR}/{需求名称}_开发冒烟用例.xlsx"
+```
+
+- 转换失败则输出 `[XMind 转换失败] 原因：...`，继续后续工作
+
+输出进度标记：`[WF:done:冒烟-导出]`
+
+---
+
+### Step 4: 模块划分与场景分组
+
+> **三方喂养**：将 Step 2 分析结论（关键风险/矛盾点/测试重点）作为本步骤的关键输入。Step 2 中识别出的矛盾/风险点，在模块划分阶段标记到对应的功能模块上。若某模块在三方分析中被标记为高风险，该模块的 P0 功能点优先纳入验证范围。
 
 按 `generation_rules.md` 中的规则执行模块划分、场景分组、校验链分组、矛盾检测。
 
-**Step 2.1：模块划分**
+**Step 4.1：模块划分**
 
 章节标题为父模块，表格每行为子模块。核心原则：章节优先 + 父级保留。配置层章节仅输出合并描述，不生成用例。检测 PRD 自身矛盾并记录。
 
-**Step 2.2：输出需求提取清单（强制）**
+**Step 4.2：输出需求提取清单（强制）**
 
 逐句扫描 PRD，提取功能点并编号，标注来源位置。每条清单必须映射到用例或待澄清问题。
 
-**Step 2.3：场景分组**
+> **与 Step 2 交叉校验**：Step 2.4 的问题映射表中「未覆盖」的严重问题，必须在清单中补充对应的功能点或标注为全局待澄清。
+
+**Step 4.3：场景分组**
 
 模块内按操作场景分组，同一场景的正常/分支/异常用例放同一分组。场景命名格式：`{操作对象}-{关键条件/动作}`。定义层与配置层场景冲突时，场景分组命名统一使用定义层体系，用例差异通过标题中的方案标识体现。
 
-**Step 2.4：校验链分组（特殊处理）**
+**Step 4.4：校验链分组（特殊处理）**
 
 顺序校验模块（提交流程、审批流程等）按校验环节分组，每个环节包含正常/分支/异常用例。第一个校验环节上方增加汇总用例，描述完整校验顺序。
 
-**Step 2.5：PRD 自身矛盾检测**
+**Step 4.5：PRD 自身矛盾检测**
 
 检测前后描述不一致、图文不一致、术语不一致、参数不一致、流程矛盾，记录到完整性检查报告，高/中影响程度同时记入待澄清问题。
 
-**Step 2.6：优先级定义**
+> **增量检测**：基于 Step 2 三方分析中已发现的问题，补充检测其余不一致之处，避免重复。
+
+**Step 4.6：优先级定义**
 
 P0=核心阻断 / P1=重要功能 / P2=次要功能 / P3=体验优化
 
+与 Step 2 的风险严重程度对齐——高风险功能点至少为 P1。
+
 详见 `generation_rules.md` → 优先级分配。
 
-### Step 3: 测试维度识别
+---
+
+### Step 5: 测试维度识别
+
+> **三方喂养**：Step 2 中三方分析标注的测试重点方向，在本步骤中优先覆盖对应维度。
 
 为每个功能点识别需要覆盖的测试维度。
 
@@ -236,9 +393,13 @@ P0=核心阻断 / P1=重要功能 / P2=次要功能 / P3=体验优化
 
 详细定义和触发判定见 `references/test_dimensions.md`。
 
-Step 2 模块划分+场景分组 和 Step 3 维度识别全部完成后，输出进度标记：`[WF:done:需求分析]`
+Step 4 模块划分+场景分组 和 Step 5 维度识别全部完成后，输出进度标记：`[WF:done:需求分析]`
+
+---
 
-### Step 4: 用例生成
+### Step 6: 全量用例生成
+
+> **三方喂养**：Step 2 中识别出的矛盾/风险/测试重点，在用例生成阶段体现为：(1) 矛盾相关的功能点生成额外边界/异常用例；(2) 高风险功能点确保覆盖 D1-D5 全维度；(3) 三方分析的待澄清问题逐条映射到用例或 Q 编号。**确保与 Step 3 冒烟用例中的 P0 覆盖相互印证。**
 
 按 `generation_rules.md` 中的规则生成用例。
 
@@ -262,12 +423,14 @@ Step 2 模块划分+场景分组 和 Step 3 维度识别全部完成后，输出
 
 全部用例生成完成后，输出进度标记：`[WF:done:用例生成]`
 
-### Step 5: 输出与校验
+---
+
+### Step 7: 输出与校验
 
 按 `output_template.md` 定义的结构输出 Markdown 文档。
 
 **输出文档结构：**
-1. 文档头部 → 2. 测试用例 → 3. 网络相关 → 4. 兼容性 → 5. 埋点（如有）→ 6. 冒烟清单 → 7. 完整性检查报告
+1. 文档头部 → 2. 测试用例 → 3. 网络相关 → 4. 兼容性 → 5. 埋点（如有）→ 6. 完整性检查报告
 
 **生成后必须执行的自检：**
 - 用例编号格式统一（tc-001-p0）
@@ -291,7 +454,7 @@ Markdown 文档生成完成后，**必须自动执行**以下转换命令：
 - 兼容 XMind 8 及后续版本
 - 转换失败则输出 `[XMind 转换失败] 原因：...`，继续后续工作
 
-XMind 转换完成后，输出最后一个进度标记：`[WF:done:导出格式]`
+XMind 转换完成后，输出进度标记：`[WF:done:导出格式]`
 
 ---
 
@@ -303,3 +466,6 @@ XMind 转换完成后，输出最后一个进度标记：`[WF:done:导出格式]
 - 测试用例数量与需求复杂度匹配，禁止为凑数生成无效用例
 - 输出文件为 `.md` 格式，使用标准 Markdown 语法
 - 主动补充的用例必须在前置条件行尾标注 `【主动补充】`
+- 串行执行，不得调换顺序：先冒烟（Step 2-3），再需求分析+全量用例（Step 4-6），最后校验（Step 7）
+- Step 2 三方分析结论必须贯穿 Step 4-6，风险点/矛盾点不得在两套产出中遗漏
+- 所有输出文件写入 `{TASK_OUTPUT_DIR}`，禁止写入沙箱外路径
